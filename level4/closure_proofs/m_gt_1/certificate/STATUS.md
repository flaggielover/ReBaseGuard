# Interval-certificate status

`NOT STARTED / NOT REQUIRED FOR THE CLAIM MADE`.

The theorem is a structural derivative identity and this campaign claims no
new rigorous scalar inequality at `m>1`. The frozen protocol therefore does not
require a new Arb certificate. In addition, the numerical gate stopped the
campaign before the Lean/certificate stages. The existing Level 1–3 `m=1`
certificate was regression-checked but is not relabelled as `m>1` evidence.
