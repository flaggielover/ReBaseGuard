# PS1 lifecycle adapter (PRE-RESULT, operational only)

The audited production lifecycle (`p5y_k1_sr_production_lifecycle_successor`, bcec064) bound to the PS1 production
namespace. `ops/` is generated by `code/make_ops_adapter.py`: six files byte-identical, `opscommon.py` differing in
exactly one line (`PARENT_NS_REL`). The runtime-state exclude block is retargeted to the PS1 namespace. The contract
(`code/make_contract.py`) changes only PS1 values: parent commit/hashes, 369 cells, 16 AWS workers on cores 0-15, no
Vultr cells, the new cap, unit prefix `rbg-p5y-k1-ps1-prod-`, and separate production/ops clones and runtime directory.

Production root: a SEPARATE local clone (`/home/ubuntu/work/ReBaseGuard-ps1-prod`, own `.git`) at the PS1 authorization
commit. Ops root: a separate local clone (`/home/ubuntu/work/ReBaseGuard-ps1-ops`) at the adapter commit.

Start (ONLY after the authorization record says YES; not run by this package):

    /home/ubuntu/work/ReBaseGuard/level4/.venv/bin/python \
      /home/ubuntu/work/ReBaseGuard-ps1-ops/level4/closure_proofs/p5y_k1_ps1_lifecycle_adapter/ops/prodctl.py start --role AWS
