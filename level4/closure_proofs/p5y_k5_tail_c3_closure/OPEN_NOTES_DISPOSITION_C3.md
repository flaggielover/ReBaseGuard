# C3 open notes carried forward

C3 is **REJECTED**; nothing from it enters the permanent record and no coverage map r6 follows. These notes are
carried for a successor. They are recorded, not repaired: the C3 packet is sealed and is not edited after
adjudication.

- **C3-N1 — `whole_cell_ok` does not check the domain of the certificate supplying `Ā`.** The check validates the
  taboo block and the denominator sub-block cover, but the whole-kernel ARL certificate that supplies `Ā` is not
  domain-checked. Inert on this campaign's data because `Ā` is inert throughout — `τ/D_lo < Ā` on every tail cell,
  so `Ā_eff = τ/D_lo` and `Ā` never binds — but a successor whose `Ā` binds must close this before relying on the
  selector. Raised by the C3 adjudicator as its gap C-1.

- **C3-N2 — `classify` and the adoption-floor evaluator are unmutated.** The adversarial suite covers the selector
  thoroughly (13 mutants, 12 killed, 1 proved equivalent) but plants nothing in the classifier or in the F1/F2
  floor evaluation, both of which are load-bearing for the verdict. A successor must mutate them.

- **C3-N3 — a successor forecast must pin its own registries and re-assert measurement gating.** C3 inherited
  C2's adopted inputs and gating rather than re-asserting them in its own protocol.

- **C3-N4 — the concrete next target is a certified lower bound on `E_a[τ]`, not a real order-3 address.** The
  adjudicator established, and this campaign verified, that cells 308 and 309 are infeasible by **any** improvement
  of `A1` and `A2`: setting both to zero still leaves Γ = +0.039568 and +0.092812. Every operator-level route must
  therefore push `A0` below **4.3752** (308) and **3.2142** (309) against certified **5.2185** and **4.8672**.
  Since `A0 = min(Ā, τ/D_lo)` bounds `E_a[τ]`, which Lemma SM(d) proves sharp, that reduction is capped by the
  truth — but **no certified lower bound on `E_a[τ]` exists in committed evidence**, so unreachability is unproven.
  Certifying `E_a[τ] > 4.3752` and `> 3.2142` is an operator-level computation requiring **no scientific address**,
  and is the only thing that can discharge the exhaustion premise. Cell 307 is different: `A0` is not its blocker,
  since at `A1 = A2 = 0` it closes with the certified `A0`.

- **C3-N5 — cell 306 remains 7.56 % of uniform reduction away from adoptability**, per the adjudicator. It closes
  under every registry-based supply and under no registry-independent one; N9 and N10 from C2 remain open.

- **Inherited and still open:** C2's **N7** (deterministic exhaustion not established), **N9** (no second
  independently written certifier), **N10** (the registry build host is recorded nowhere).
