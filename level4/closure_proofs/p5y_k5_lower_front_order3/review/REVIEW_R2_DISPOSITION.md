# Disposition of pre-freeze review r2 (verdict NOT_READY)

| id | severity | disposition |
|---|---|---|
| B-R2-1 | BLOCKING | Fixed in `code/tc_consume.py::load_protocol`: the protocol is read with `committed_bytes`, must equal `git show <freeze>:config/TC_PROTOCOL.json`, the namespace must have no uncommitted or untracked change and no committed change outside `evidence/tc_r1/` since the freeze, and every protocol pin must match (the producer's `frozen_guard` logic). `governance_context` requires the committed QUALIFICATION_RESULT (QUALIFIED, this protocol, S00 head = freeze) and AUTHORIZATION bound to this protocol sha, its addresses and that qualification's sha. The producer binding now carries `protocol_sha256`, checked by `check_binding`. The r2 demonstration (pin-consistent working-tree edit of tc_rule + protocol) is qualification case S08(l)T1; committed edits are T2 (index) and T4 (rule). |
| B-R2-2 | BLOCKING | Fixed: `tc_qualify.py` pins the four thread variables and `K1_THREADS_PINNED` before any import that could load numpy; S02 records the environment and requires `K1_THREADS_PINNED == "1"`. Verified by a full dress rehearsal of the qualification on the vultr dev clone (dev-only commits, never pushed; result in `evidence/dev/`). |
| N-R2-1 | NOTE | S06 now runs the replay with `--protocol-sha256`, which executes `runtime_checks` (runtime, both manifests, K1 record sha, frozen module set before and after) and requires its result. S08(l) `code/tc_lifecycle_sim.py` walks the §8 commit sequence in a throwaway clone: `require_authorized` positive (address) and negative (non-address), `tc_consume` (non-replay) positive on 34 synthetic no-op records with pass sets unchanged and 136 cross-check comparisons, and four tamper cases refused. |
| N-R2-2 | NOTE | `module_check` runs before and after the computation: every loaded repository module must be pinned by the protocol or listed in the Aux5 or order-3 producer manifest, with a matching sha256. The post-compute count is recorded. |
| N-R2-3 | NOTE | Protocol `published_order3` and the authorization brief now list `sup.G` (certified candidate supremum); `G_at_a` removed from `fields`. |
| N-R2-4 | NOTE | The consumption output records `verified` (TC_INDEX sha, the governance context, acceptance counts), `freeze_commit` and `crosscheck_comparisons`. |
| N-R2-5 | NOTE | Tightened: the run head must descend from the AUTHORIZATION add-commit and have, at that commit, the bound ALLOW guard and the same AUTHORIZATION; GUARD must be DENY at consumption; AUTHORIZATION and every sealed file (cells, repro, index, ledger) must be committed once and never modified; the ledger must contain exactly one START and one ok OUTPUT per address with the index sha, a RUN_END with identical reproduction, and no RUN_VOID/CAP_STOP. |
| N-R2-6 | NOTE | Accepted as non-blocking (r2: the tower is mathematically correct). The r ≥ 1 source tower and the W assembly remain protected by exact equality with the independent `tc_crosscheck` (M18–M20 detected). Disclosed. |
| N-R2-7 | NOTE | Added S08 (i) rule bytes ≠ pin refused, (j) null binding refused, (k) positive control of `check_index`; docstring corrected (20 mutants; all S08 cases listed). |
| N-R2-8 | NOTE | All review documents (briefs, r1, r2, both dispositions) are committed and pinned in the freeze commit; the authorization review writes to `evidence/tc_r1/`. `repro/` and the ledger are in `expected_outputs` and `seal_rule`. |
| N-R2-9 | NOTE | `tc_run` no longer accepts `--workers` (the protocol's frozen count). The governance gate moved inside `compute(mode="real")`. |
| N-R2-10 | NOTE | Operational: S02 is part of the qualification immediately before the authorization; the run starts right after. Host drift during the run would refuse remaining cells (VOID, fail-closed). No system setting is changed by this campaign. |
| N-R2-11 | NOTE | Reworded: the consumer enforces one indexed record and exactly one START/OUTPUT ledger pair per address. |
