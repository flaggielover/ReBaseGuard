# Proof Track 3 — general location-family derivative theorem

This scoped campaign determines the regular location-family stopped-score
identity behind matched `m=1` ReBaseGuard reuse.

The outcome-blind definition audit establishes the parameter score
`s=f'/f=-psi` under `Z=epsilon-e` and identifies the actual reuse gain as

```text
Gamma_f=E[Z_tau sum psi(Z_t)].
```

This is neither of Stage D's two non-Gaussian candidate estimands except in the
Gaussian case.  Historical Stage-D t3 remains `AMBIGUOUS`; no old artifact is
rewritten.

Current gate: protocol freeze, before new confirmatory outcomes.

