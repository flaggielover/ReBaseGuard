# P4X CHECKPOINT A — BINDING SUCCESSOR SPECIFICATION

```text
ARTIFACT                = P4X_CHECKPOINT_A
ACTIVE                  = YES
BINDING                 = YES
SOURCE COMMIT           = b3f050bcfb1c8b908e50376b4bf6d6464871da13
P4_ORIGINAL_VERDICT     = PARTIAL            (immutable, forever)
P4X_SUCCESSOR_VERDICT   = NOT_YET_RUN
NOVELTY_STATUS          = NOT_ESTABLISHED
P5_RESIDUAL_STATUS      = DOCUMENTED_LIMITATION
LEVEL4_GLOBAL_CLOSURE   = NO
```

This document and `results/checkpoint_a.json` are the binding specification of
the P4X successor campaign.  The JSON is authoritative where the two disagree;
it is generated by `build_checkpoint.py`, which derives every frozen number
from artifacts that already exist rather than transcribing them, and which
`tests/` re-runs and compares byte for byte.

**No production has been run.  No result artifact exists.  No production
outcome has been inspected.**

## 1. Governance

`P4_ORIGINAL_VERDICT = PARTIAL` and is immutable.  Historical P4 artifacts —
its ten failed cells, its sixteen `COUNTEREXAMPLE-NOT-DEMONSTRATED` cells, its
three failed gates and its `closure_decision.json` — are never edited,
regenerated or reinterpreted.  P4X is a **successor campaign only**.

### 1.1 The destroyed disposition audit

`level4/closure_proofs/p4_final_disposition_audit/` is **destroyed and
unrecoverable**.  Its surviving content digest is

```text
sha256  bda05c9c5ee5df2a7bfbe11ca1fb07432907378299fd36ea0b75cada68ffba34
```

recorded in P5X's pre-campaign integrity manifest.  It was an untracked
namespace, was never committed, and was removed by an external `git clean`
alongside commit `31132e8` (recorded in
`p5x_global_nonlinear_dynamics/INCIDENT_EXTERNAL_TREE_CHANGE.md` §1(b)).

**P4X does not claim to inherit its wording.**  Its text cannot be read and is
not guessed.

### 1.2 Governance derived independently

The authority for opening P4X is derived from surviving repository evidence
alone:

1. The parallel **P5** disposition ruling, quoted verbatim in the surviving
   `p5x_global_nonlinear_dynamics/FEASIBILITY_AUDIT.md`:
   `P5_PARTIAL_SHOULD_BE_FINAL`, `P5R_LAUNCHED = NO`,
   `NEW_SCIENCE_REQUIRED = YES`.  It ruled the missing work **new science
   belonging to a new priority** — not forbidden work.
2. **P5X was opened as a successor under that ruling**, was committed, ran
   through eleven checkpoints and was archivally completed, with no objection
   recorded anywhere.
3. P5X's own final disposition audit (§13) names this campaign explicitly:
   *"P4X and residual P5 coexist: P4X is not the only remaining repair
   campaign."*
4. P4's own `CLOSURE_REPORT.md` §10 limitation 1 names *"a follow-up campaign"*
   as the fix for one of its own limitations.
5. A full repository search for `P4R`, `P4.1`, `p4_1` returns **zero**
   surviving prohibitive text.

```text
GOVERNANCE_READING = no retroactive repair or amendment of the historical
                     priority; a successor campaign under a fresh
                     preregistered scope is permitted
```

**Precedence rule.**  If an artifact hashing to `bda05c9c…` is ever recovered
and it forbids a successor campaign, that ruling takes precedence over this
inference and **P4X stops**.

## 2. Successor scientific question — frozen

> Can the already-proved and independently adjudicated Priority-4
> location-family derivative theorem be supported by numerical evidence whose
> precision and statistical semantics are correctly matched to the original
> scientific claims, **without changing the theorem or its scientific scope**?

P4X is **not** a stronger-theorem campaign.

## 3. Inherited theorem — frozen, unchanged

From `../../p4_theory_generalization/THEOREM.md`, tree object
`eede90383da44c250871b1bb97d12045c897c8d9`:

```text
g_m'(0) = -Gamma_{D,m,f}

Gamma_{D,m,f} = E_0[ A_m sum_{t<=tau} psi(Z_t) ],     psi = -f'/f

F'_{rho,m}(0) = rho (1 - Gamma_{D,m,f})

A_m = (1/w) sum_{r=0}^{w-1} Z_{tau-r},   w = min(m, tau)
Z_t = eps_t - e,   f_e(z) = f(z+e)

for every fixed m >= 1, under the historical assumptions (A1)-(A7)
```

Companions `G1'`, `G2`, `G3a` (in its narrowed form) and `G4` are inherited
unchanged.

```text
THEOREM_STRENGTHENING_PERMITTED = NO
THEOREM_REPROVING_PERMITTED     = NO
```

## 4. Assumption semantics — frozen

* `(A1)-(A7)` are **sufficient** assumptions.
* **No necessity claim is added** by P4X.
* **A3 sharpness** is already separately proved (`PROOF.md` §9, exact defect 2)
  and Arb-certified as an exact rational.  P4X does not re-derive it.
* **A5's first-moment boundary** is an analytic **non-existence** result
  (`PROOF.md` §10, `E|A_1| = infinity`) — not a false-identity claim.

```text
P4X MUST NOT require arbitrary failure demonstrations outside all sufficient
assumptions, and MUST NOT test necessity that no frozen claim asserts.
```

## 5. Core successor obligations — frozen

| id | obligation |
|---|---|
| `C1` | inherit the theorem unchanged |
| `C2` | attainable-precision numerical correspondence |
| `C3` | Route Q only as an independent cross-check |
| `C4` | failure-mode evidence matched to the actual proved failure mode |
| `C5` | Gaussian consistency by a two-sample uncertainty statistic |
| `C6` | re-verify the inherited Lean and Arb artifacts |
| `C7` | protected-tree integrity |

## 6. Estimator plan — frozen

```text
Route A   the frozen Priority-4 score estimator, unchanged
Route B   the frozen Priority-4 CRN central difference, unchanged
fd_steps  h = 0.05 / 0.025
Richardson (4 D(h/2) - D(h)) / 3, formed PER BLOCK
VARIANCE REDUCTION ADOPTED = NONE
```

All candidate variance-reduction methods tested during R0 were **rejected**,
with measured cause:

| candidate | measured outcome |
|---|---|
| reflection-antithetic | pathwise exact (`0.000e+00`), distributionally valid for symmetric families — but VRF **0.001-0.003**, a 300-1000x variance **increase**: substituting the mirror for the `-h` run destroys the CRN cancellation |
| Corollary-G2 control variate | **degenerate**: per-path variance `6e-29` to `9e-31`, i.e. exactly zero — which is Corollary G2's own content |
| coarse finite-difference step | **inadmissibly biased** for `skewnormal4` (`+4.94` and `+33.01` baseline SEs); for `t1p5` the benefit is real but the bias is unresolved and sign-inconsistent |
| fine finite-difference step | variance **increase**, factor 0.32-0.55 |

Evidence: `../r0_variance_reduction_pilot/results/pilot.json`.

## 7. Heavy-tail policy — frozen

`t1p5` is the **only** family requiring `alpha < 2` handling.

```text
measured alpha (t1p5, both routes, all four layer/detector combos) = 1.471 - 1.528
measured alpha (every other theorem-supported family, minimum)     = 2.695

frozen planning alpha for t1p5   = 1.47      (conservative floor of the range)
kappa = 0.5              if alpha >= 2
kappa = 1 - 1/alpha      if alpha <  2       => kappa(t1p5) = 0.319728

MINIMUM_BLOCK_PATHS = 250 000   where alpha < 2
                    =  20 000   otherwise
```

Model justification, independent of the measurement: Student-`t` with
`nu = 1.5` has tail index exactly `1.5`, and the frozen Priority-4 protocol
already records this family as *"finite mean, INFINITE variance"*.  The
precision limit is a property of the model, not of the implementation.

## 8. Precision rule — frozen

```text
frozen accuracy criterion   0.03      (inherited unchanged from Track 3)
frozen consistency criterion |z| <= 4 (inherited unchanged)

r* = 0.010823        derived from   1.96 * sqrt(2) * r* = 0.03
                     exact value    0.010823063...

N_required = N_ref * (relSE_ref / r*)^(1/kappa)
```

`r*` is **forced by the unchanged 3 % criterion**, not chosen.  P4X does not
weaken the 3 % tolerance; it buys the precision at which that unchanged
tolerance becomes attainable.

### 8.1 Two-stage allocation

```text
Stage 1   planned allocation at the classical rate kappa = 0.5, from the
          historical reference relative standard error.

Stage 2   AT MOST ONE top-up, triggered ONLY when a route's own ACHIEVED
          relative standard error on a cell exceeds r*.  Sized by the same
          rule at the frozen heavy-tail kappa.  Fresh independent blocks,
          pooled with stage 1.
```

The trigger is **a route's own achieved standard error**, computed without
reference to the other route.  The trigger is **not**:

* the observed Route-A minus Route-B discrepancy;
* the sign or direction of any disagreement;
* whether a cell is "close" to passing;
* any pass/fail outcome.

**Disclosed limitation.**  A precision-triggered top-up is a sequential design,
so the pooled standard error carries an `O(1/B)` optional-stopping bias.  At
the block counts planned here that bias is negligible relative to `r*`, but it
is disclosed rather than assumed away.

## 9. Production scope — frozen

Exactly the historical Priority-4 theorem-supported scope.  Not broadened.  Not
to be narrowed after results.

```text
layers      frozen  (cusum@5, sr@520.886133602749),  max_steps 200 000
            reduced (cusum@2, sr@20),                max_steps  60 000
detectors   cusum@2, cusum@5, sr@20, sr@520.886
families    THEOREM-SUPPORTED   gaussian, laplace, logistic, skewnormal4, t1p5, t3
            OUTSIDE-ASSUMPTIONS uniform, cauchy
m grid      1, 2, 3, 5
cells       96 theorem-supported  (2 layers x 2 detectors x 6 families x 4 m)
            32 outside-assumption
configurations                    24
routes      A (score), B (Richardson CRN central difference),
            Q (deterministic quadrature, memoryless detector, 24 rows),
            N (deterministic-stopping neutrality control, 72 rows)
```

The four windows of a configuration share their paths, so cost is charged once
per `(layer, detector, family, route)`.

## 10. Gates — frozen

| id | gate |
|---|---|
| `X1` | protocol and witness hashes match the manifest |
| `X2` | inherited theorem tree object unchanged (`eede9038…`) |
| `X3` | Route Q analytic identity, worst relative discrepancy `<= 1e-6` over 24 rows |
| `X4` | Route Q uniform failure as predicted: score side exactly `0`, exact slope reproduced |
| `X5` | Route N neutrality: all 72 deterministic-stopping cells return gain `1`, `\|z\| <= 4` |
| `X6` | **theorem-supported correspondence**: relative discrepancy `<= 0.03` **and** `\|z\| <= 4`, on all 96 cells, after each route reaches `r*` or is declared `PRECISION_LIMITED` |
| `X7a` | A3 / moving support: discharged by the existing closed form, Route Q and the exact rational Arb certificate.  Monte Carlo is corroborating, not load-bearing.  **New compute: NONE** |
| `X7b` | first moment: **non-existence**, discharged by `PROOF.md` §10.  **No Monte Carlo "large disagreement" signature is required.**  **New compute: NONE** |
| `X8` | both frozen detectors covered |
| `X9` | at least five theorem-supported families |
| `X10` | asymmetric family: origin not a fixed point; never classified at `0` |
| `X11` | **Gaussian consistency** by the two-sample statistic, 8 cells, limit 4 |
| `X12` | inherited Arb certificates re-verify at 160 and `>= 256` bits |
| `X13` | inherited Lean re-verifies: 19 declarations, axioms exactly `propext`, `Classical.choice`, `Quot.sound` |
| `X14` | protected-tree integrity, recorded pre and post |
| `X15` | no historical mutation; `P4 = PARTIAL` unchanged |

### 10.1 `X6` may fail

The 3 % scientific tolerance is **not weakened**.  Production must purchase the
frozen precision **before** adjudicating `X6`.  If the purchased-precision
estimates still disagree materially, **the gate must be allowed to FAIL** and
must be reported as failing.

### 10.2 `X11` — the two-sample statistic

```text
z_combined = |estimate_1 - estimate_2| / sqrt(SE_1^2 + SE_2^2)   <= 4
```

using **both** campaigns' published uncertainty — the closed campaigns'
`gamma_tilde_se` from the frozen Priority-3 stability map, and P4X's own.
Neither Monte Carlo estimate is treated as exact.  The historical single-error
statistic is **reported alongside and gates nothing**.

This is a **new preregistered object**.  It does not repair P4's gate; P4's
gate remains failed and untouched.

## 11. Route-Q role — frozen

```text
ROUTE_Q_ROLE = INDEPENDENT_CROSS_CHECK_ONLY
```

Route Q evaluates the memoryless detector `tau = inf{t : |Z_t| >= c}` at
`c = 2.0`.  The frozen Priority-4 `EVIDENCE_BOUNDARY.md` §3 states that nothing
in Route Q is evidence about `h = 5` or `A = 520.886133602749`.

Therefore Route Q **may not** arbitrate detector-specific P4 production
evidence, and **may not** serve as a control variate.  The feasibility audit's
draft `X6` arbitration clause was withdrawn during R0 and **remains withdrawn**.

## 12. Lean and Arb — frozen

```text
NEW LEAN DECLARATIONS  = NOT PERMITTED
NEW ARB OBJECTS        = NOT PERMITTED
OBLIGATION             = re-verification of the inherited artifacts only
```

Inherited: 19 Lean declarations with axioms exactly `propext`,
`Classical.choice`, `Quot.sound`; 3 Arb objects at 160 bits.

## 13. Cost envelope — frozen

```text
TOTAL_CPU_CAP             = 60 CPU-hours
PER_CONFIGURATION_CPU_CAP = 40 CPU-hours

projected stage-1 total       =  2.91 CPU-hours
projected worst-case total    = 59.08 CPU-hours
SILENT EXTENSION = NOT PERMITTED.  Breach action = STOP.
```

### 13.1 Pre-registered cost risk — stated before any result

The worst-case projection is dominated by a single `(configuration, route)`:

| configuration | route | stage 1 | worst case | vs 40 h cap |
|---|---|---|---|---|
| `frozen/sr@520.886/t1p5` | B | 1.756 h | **55.84 h** | **EXCEEDS** |
| `frozen/cusum@5/t1p5` | B | 0.185 h | 1.72 h | ok |
| `reduced/cusum@2/t1p5` | B | 0.084 h | 0.45 h | ok |
| `reduced/sr@20/t1p5` | B | 0.061 h | 0.24 h | ok |
| every other configuration | — | ≤ 0.10 h | ≤ 0.10 h | ok |

**Therefore, pre-registered now and not discovered later:**
`frozen/sr@520.886/t1p5` Route B is **at risk of being declared
`PRECISION_LIMITED`**.  If, after stage 1, its projected top-up cost exceeds the
40 CPU-hour per-configuration cap, it is declared `PRECISION_LIMITED` **from
projected cost alone, before its gate is adjudicated**, and recorded as a
documented limitation.  In that event `C2` is incomplete and **P4X cannot be
`CLOSED`; the honest outcome is `PARTIAL`.**

The declaration may **never** be made after seeing a result.

R0 observed that at a 320 000-path block size this configuration's Route-B
estimate sat within `0.16` combined standard errors of the frozen Route-A
value.  That is an observation about estimator behaviour under adequate block
size.  **It is not a prediction that this cell will pass**, and this checkpoint
does not treat it as one.

## 14. STOP rules — frozen

Production stops immediately on any of:

1. **estimator implementation drift** — any deviation of Route A or Route B
   from the frozen Priority-4 implementation;
2. **protected-tree mutation** — any tracked path outside the P4X namespace
   differing from `HEAD`;
3. **precision policy mismatch** — any sample-size decision not produced by the
   frozen `N_required` rule;
4. **unapproved route substitution** — any use of Route Q as an arbiter, or any
   adopted variance-reduction method;
5. **cost-cap breach** — total `> 60` CPU-hours, or a configuration `> 40`;
6. **inability to reproduce historical anchors** — the frozen Route-Q, Route-N,
   Lean or Arb results failing to re-verify;
7. **post-result optimisation** — no estimator, threshold, budget or scope
   change after seeing a production `FAIL`.

## 15. Verdict semantics — defined before results

```text
P4X = CLOSED    iff all binding successor obligations C1-C7 pass

P4X = PARTIAL   iff the theorem remains valid,
                    some evidence obligations remain incomplete or fail,
                    and no theorem contradiction is established

P4X = FAIL      iff a load-bearing scientific claim is contradicted,
                    or an integrity or governance failure invalidates
                    the campaign
```

No post-result intermediate vocabulary may be introduced.

## 16. Scientific-line semantics — frozen

If `P4X = CLOSED`:

```text
P4_ORIGINAL_VERDICT   = PARTIAL
P4X_SUCCESSOR_VERDICT = CLOSED
P4_SCIENTIFIC_LINE    = CLOSED_BY_SUCCESSOR_CAMPAIGN
```

**Original P4 remains historically `PARTIAL` forever.**

## 17. Novelty

```text
NOVELTY_STATUS = NOT_ESTABLISHED
```

P4X performs no novelty work.

## 18. Level-4 context

```text
P5_RESIDUAL_STATUS    = DOCUMENTED_LIMITATION
LEVEL4_GLOBAL_CLOSURE = NO
```

P4X is not the only residual Level-4 limitation.  A P4X closure would imply
neither P5's closure nor Level-4 global closure.

## 19. Protected-tree manifest

33 tracked paths are frozen for the duration of the campaign and recorded by
git object in `results/checkpoint_a.json` under `protected_tree_manifest`,
including all of `P1`-`P9`, `P8R`, `P9R`, `P5X`, `p4_theory_generalization`,
`rebaseguard-lean`, `rebaseguard-proof`, `closure`, `Mathematical_proof` and
the root `README.md`.

Every tracked path outside
`level4/closure_proofs/p4x_generalization_boundary/` is immutable.
