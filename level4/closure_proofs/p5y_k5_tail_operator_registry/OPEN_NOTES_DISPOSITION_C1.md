# C1 disposition of the carried-forward review notes (N1, N3, N5) and of the repaired (P3′) logic

Written before the C1 freeze. No predecessor namespace is edited; any cross-reference is owned here.

## N1 / C7 — the order-3 producer's registry says no real CUSUM cell may be evaluated by it

**NOT LOAD-BEARING for C1. No authorization bridge is needed, and none is issued.**

C1 evaluates no order-3 quantity of any kind. Its own contribution — the tail operator registry — is produced by
`taboo_certify`, which forms the kernel K_e, its atom split, the one-step alarm probability h₁ and polynomial
supersolutions, and touches no source S_r, no candidate of F / D / H / G, no K1 record and no value of R. The
order-3 producer namespace is never imported, its gated entry points (`certify_real_cell`,
`rung3_engine.certify_order3`, `rung3_residual.g_residual`) are never reached, and `NEW_REAL_ADDRESSES = 0`.

C1 does consume Campaign B's measurement records, whose `eps_src[3]` is an adopted **Aux3** midpoint source error —
Aux3 evidence, not order-3 producer output, and already inside the frozen producer's 262-field identity gate.

**The obligation is carried forward unchanged.** Campaign A's adoption obligation still stands and is still
unsatisfied: the registry sentence, read alone, misleads about cells 11–44, and the cross-reference must be owned by
the first successor that evaluates a real order-3 CUSUM cell. C1 is not that successor, so C1 does not discharge it.
Recorded again so the obligation survives C1 as it survived Campaign B.

## N3 — the manufactured oracle covers r = 0 and m = 1; the r ≥ 1 source tower and the W assembly rest on cross-checks

**Unchanged in substance by C1, and C1 does not widen it.**

C1 alters no source-tower quantity and no W enclosure. σ₃, σ₄, the h-tower, `eps_src[*]`, `H_at_a` and `W2` are
exactly Campaign B's, byte-for-byte, and their disposition is Campaign B's: the frozen J/h tower intersected with
adopted Aux3 order-3 evidence, plus the derived identity gate that rebuilds the record's `R2_interval` from `H_at_a`
and `W2`. C1 substitutes only A0, A1, A2, which enter at theorem TC §4 step 3.

What C1 **adds** on this front is a second, locally re-runnable check of the part that matters here: the
`c1_mutations.py` suite exercises the source-node swap, the W endpoint swap, the dropped order-3 residual term, the
Taylor-coefficient error and the midpoint/whole-cell confusion against the committed evidence with stdlib Python
only — no python-flint, no remote host, no K1 records — so a reviewer or an adjudicator can re-run it. Campaign B's
manufactured oracle could not be re-run locally at all.

**Still open, and still scoped to a real-Ĝ successor**: manufactured fixtures at the tail geometry. C1 does not need
them because it proposes no candidate of F; a route that does will.

## N5 — the four new order-3 fields have no identity gate

**Remains RETIRED under C1, for the same reason and by the same construction.**

C1 inherits Campaign B's route TCT0 unchanged: Ĝ := 0, so `sup.G = 0` and `abs_G_at_a = 0` exactly, `delta_G` is the
closed-form combination `3k₁s_H + 3k₂s_D + k₃s_F + σ₃` of identity-gated quantities, and `eps_src[3]` is the adopted
Aux3 value. C1 adds no order-3 field. The structural widening argument the Campaign-A adjudicator described is
therefore still not merely sufficient but vacuous here, and it is **re-verified** rather than assumed: the C1
consumer passes `order3 = None` on every call, and mutant `M08_drop_order3_residual_term` confirms the order-3
residual term is load-bearing and detected when removed.

The new trust surface C1 *does* open is a different one, and it belongs to the operator side: the six certified
constants per cell. It is gated by `c1_tail_registry verify` (every artifact recomputed from its own exact payload,
every certified field compared) and by the independent Lemma Dv′ re-derivation in `c1_forecast`. It is **not** gated
against any prior adopted evidence, because none exists for the tail domain — that is the honest statement of C1's
residual trust boundary, and it is the direct analogue of what N5 said about Campaign A's order-3 fields.

## The repaired (P3′) logic — no midpoint quantity where a whole-cell bound is required

Preserved, and re-tested. Campaign B review r1 note N1 found that an adopted *midpoint* bound of ‖h_j‴‖ had been fed
into a recursion that theorem TC premise (P3) needs uniformly on the cell. The repair — a midpoint tower for σ₃ (a
(P2) premise, at e₀) and a cell tower carrying the mean-value correction ρ·sup_C‖h_j⁗‖ for σ₄ (a (P3) premise) — is
inherited byte-for-byte and is exercised by two mutants: `M01_midpoint_tower_used_for_sigma4` and
`M02_omit_mean_value_correction`, both detected.

C1's own constants introduce no midpoint quantity at all: each of Ā, τ, C_T, D_lo, D1, D2 is certified **uniformly on
the cell's own e-interval** by Lemma T with E = that interval, never at e₀. The block geometry (one block per cell,
equal to the cell) is what makes that exact rather than approximate.

## Summary

| note | load-bearing for C1? | disposition |
|---|---|---|
| N1 / C7 | no — no order-3 quantity of any kind is evaluated | obligation carried forward unchanged, still assigned to the first successor that evaluates a real order-3 cell |
| N3 | no — C1 alters no source-tower or W quantity | Campaign B's disposition inherited; C1 adds a locally re-runnable adversarial suite; tail fixtures still scoped to a real-Ĝ successor |
| N5 | no — retired by construction, and re-verified | C1 opens a *different* trust surface (the six operator constants), gated by artifact re-verification and an independent Lemma Dv′ path, and stated openly as un-gated against prior adopted evidence |
| (P3′) repair | yes — inherited | preserved byte-for-byte, re-tested by two mutants; C1's own constants are whole-cell by construction |
