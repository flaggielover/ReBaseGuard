# K5 Campaign B — the CUSUM m = 5 tail (K1 cells 305–309)

Additive namespace. Start: `p5y-postk1-frontier` after the theorem-TC lower-front successor (m = 1, 2, 3 closed on
0–309; m = 5 open only on 305–309). Nothing outside this namespace is modified; AWS SR/PS1 untouched; `main` untouched.

| phase | where | state |
|---|---|---|
| A. tail blocker audit | `phase_a/TAIL_BLOCKER_MAP.json`, `phase_a/TAIL_BLOCKER_AUDIT.md` | done: the blocker is the whole-cell curvature width; M must fall by 1.12× (305) … 2.67× (309) |
| gates (frozen before any forecast) | `config/FEASIBILITY_GATES_B.json` | frozen |
| B. route comparison + costed plan | `phase_b/TAIL_ROUTE_COMPARISON.md` | done: T2 (theorem TC at the 5 tail cells) selected, class USEFUL, ≈ 2.2 CPU-h new real |
| C. execution | — | **NOT STARTED**: no protocol frozen, no address evaluated, guard DENY |

K5 stays PARTIAL while these five cells are open.
