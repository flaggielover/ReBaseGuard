{
 "schema": "rebaseguard.p5y.k5.cusum-real-point-executor.real-input-guard.v1",
 "policy": "DENY",
 "rule": "REAL_INPUT_ARITHMETIC_GUARD. The only recognized policy is DENY; every other value, or a missing or unreadable file, is also a denial. A backend with real_input = True is refused before any backend method runs. Enabling real arithmetic requires a reviewed successor that adds an externally authorized path; editing this file cannot enable it."
}
