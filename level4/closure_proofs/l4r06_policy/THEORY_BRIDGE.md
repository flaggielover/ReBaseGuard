# Theory-to-policy bridge

Track 1B and D4 establish the local deterministic multiplier

`F'_{rho,m}(0) = rho (1 - GammaTilde_m)`

and the corresponding boundary `rho_c(m)`. This campaign does not prove a new
theorem and does not treat that boundary as an operational phase transition.
It uses the frozen lower 95% confidence endpoint as a conservative control
signal. Because P3 satisfies `rho <= 0.8 rho_c,L95` before the unit cap, its
uncertainty-aware multiplier bound is at most `0.8`; under saturation the
actual `rho/rho_c,L95` remains below `0.8`.

This bridge establishes stability-awareness only. Monitoring consequences are
separate empirical gates H6-2 through H6-4 and the absolute-delay safety guard.
Reference-state improvement by itself cannot close L4R-06.
