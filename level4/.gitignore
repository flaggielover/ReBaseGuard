# Level 4 generated data.
#
# Rule: raw Monte Carlo tables are regenerable from the tracked manifest +
# config + seeds, and are far too large for Git.  Everything needed to audit or
# re-run a result -- manifests, configs, processed summaries, figures, reports
# and the ledger -- IS tracked.
results/raw/**/*.parquet
.venv/
__pycache__/
