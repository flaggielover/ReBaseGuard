# Final research synthesis implementation plan

1. Inventory the terminal closure decision, requirement ledger, claim firewall,
   theorem/proof artifacts, policy campaign, operational-crossing result,
   external-validation campaigns, novelty audit, and final reproducer. Record
   authoritative paths and load-bearing numerical values without running new
   science.
2. Create `docs/research_synthesis/README.md` as a concise reviewer hub and add
   the nine focused companion documents from the approved design. Keep theorem
   detail in `MAIN_THEOREM_ARCHITECTURE.md`, wording authority in
   `CLAIM_CATALOG.md`, and exact navigation in `REPOSITORY_MAP.md`.
3. Add a synthesis-only verifier that checks all cited repository-relative
   paths, terminal verdict/tallies, L4R-13, the SR evidence boundary, prohibited
   overclaims, and the Git diff allowlist. The allowlist must reject changes to
   frozen scientific or historical artifacts.
4. Add focused tests for the verifier and run them without Monte Carlo, new
   datasets, protocol changes, or network-dependent science.
5. Run the terminal offline reproducer and any repository verification required
   by current conventions. Recheck every load-bearing numerical claim against
   its authoritative JSON/report source.
6. Inspect the complete diff and confirm it contains only the approved design,
   plan, synthesis namespace, and presentation-only guard artifacts.
7. Commit implementation as `Add final ReBaseGuard research synthesis`, verify
   `origin/main` is an ancestor, fast-forward push without force, and confirm a
   clean tree with local and remote HEAD synchronized.
